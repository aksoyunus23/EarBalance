name: Build EarBalance APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK
      # Hatalı değişkenleri tamamen etkisiz hale getiriyoruz
      env:
        _JAVA_OPTIONS: ""
        JAVA_TOOL_OPTIONS: ""
        JAVA_OPTS: ""
      run: ./gradlew assembleDebug --no-daemon -Dorg.gradle.jvmargs="-Xmx2g" --stacktrace

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: EarBalance-APK
        path: app/build/outputs/apk/debug/*.apk
        retention-days: 30
